# Time Converter

1) Clone the code using git url
2) Go to the base directory and if maven is installed then run mvn test. 
Main method can be run after giving prefered input as well. Class is "com.example.formatter.SampleRunner".

NOTE: Maven 3.3.9 and Java 1.8.0 were used.
