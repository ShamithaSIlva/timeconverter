# timeconverter

1) Clone the code using url
2) Go to the base and if maven is installed then run mvn test. 
Main method can be run after giving prefered input as well. Class is "com.example.formatter.SampleRunner".
